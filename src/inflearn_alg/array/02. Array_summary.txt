1. 소수 구하기 : 에라토스테네스 체
    배열을 만들고, 배열의 인덱스를 2부터 하나씩 증가하면서
    소수인 수 (배열 내의 값이 0)를 만나면, 그 수의 배수를 다른 수로 변경시키면서
    범위 내에서 소수를 찾는 방법

2. 숫자 뒤집기 concept
    int tmp = 123;
    int t = tmp % 10;
    tmp = tmp / 10;
    int res = 0;
    res = res * 10 + t;

3.